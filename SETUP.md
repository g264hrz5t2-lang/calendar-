# セットアップ手順（GitHub）

ターミナルもgitコマンドも使いません。すべてブラウザ上の操作で完結します。
所要時間は15分程度です。

---

## 1. GitHubアカウントを作る

すでにお持ちなら飛ばしてください。

1. https://github.com/signup を開く
2. メールアドレス・パスワード・ユーザー名を入力
3. 届いた確認コードを入力

無料プランで問題ありません。

---

## 2. リポジトリ（置き場）を作る

1. 右上の **＋** → **New repository**
2. 以下を設定する

   | 項目 | 設定 |
   |---|---|
   | Repository name | `cinema-schedule`（好きな名前で可） |
   | 公開設定 | **Public** を選ぶ |
   | Add a README file | **チェックしない** |

3. **Create repository** を押す

> **Publicにする理由**：無料プランでGitHub Pages（できたページを見られるようにする機能）を
> 使うにはPublicである必要があります。中身は上映スケジュールなので公開して差し支えありません。
> 気になる場合は手順6の下の注記を読んでください。

---

## 3. ファイルをアップロードする

作ったリポジトリの画面で **uploading an existing file** というリンクを押します。
（見当たらなければ **Add file** → **Upload files**）

**`cinema-automation` フォルダの中身**を、フォルダごとドラッグ＆ドロップします。
アップロードするのは次の8点です。

```
build.mjs
template.html
theaters.json
package.json
package-lock.json
README.md
.gitignore
.github/            ← フォルダごと。中の workflows/build.yml が重要
```

> **ここが一番つまずきやすい箇所です。** `.github` フォルダが抜けると自動実行が動きません。
> アップロード画面のファイル一覧に `.github/workflows/build.yml` が出ているか必ず確認してください。
> ドット始まりのフォルダはOSによって隠しファイル扱いになり、選択できないことがあります。
> その場合はMacなら `Command + Shift + .` 、Windowsならエクスプローラーの表示設定で
> 隠しファイルを表示してからドラッグしてください。

下部の **Commit changes** を押すと保存されます。

---

## 4. GitHub Pages を有効にする

1. リポジトリ上部の **Settings** タブ
2. 左メニューの **Pages**
3. **Source** を **GitHub Actions** に変更する

> Deploy from a branch ではなく **GitHub Actions** です。ここを間違えると公開されません。

---

## 5. 手動で1回動かしてみる

自動実行を待たず、動くかどうかをここで確認します。

1. リポジトリ上部の **Actions** タブ
2. 左に **上映スケジュール更新** が出ているので選ぶ
3. 右側の **Run workflow** ボタン → もう一度 **Run workflow**
4. 1〜2分待つと、実行結果が一覧に出る

**緑のチェックが付けば成功です。** 実行をクリックして `build` → `npm run build` を開くと、
ログにこう出ているはずです。

```
■ 2026.08.10（月）のスケジュールを取得します
  ✓ ル・シネマ  5日分 / 10作品
  ! 神保町 キャッシュ検出: 先頭が 7/22（火）— 再取得 1/4
  ✓ 神保町  6日分 / 4作品
  ...
■ dist/index.html を生成しました（13館）
```

**このログは必ず一度目を通してください。** 館数が13より少なければ取得漏れがあります。
`△ 取得できなかった館` の行が出ていないか確認してください。

---

## 6. できたページを見る

**Settings** → **Pages** に戻ると、上部にURLが表示されています。

```
https://（あなたのユーザー名）.github.io/cinema-schedule/
```

これをスマホで開いて、ホーム画面に追加しておけばアプリのように使えます。
（Safariなら共有ボタン →「ホーム画面に追加」）

> **Publicがどうしても嫌な場合**：Privateリポジトリでも Actions は動きますが、
> Pagesでの公開はGitHub Proが必要になります。その場合はPagesを使わず、
> 生成された `index.html` をActionsの成果物（Artifacts）からダウンロードする形になります。
> 毎朝ダウンロードするのは現実的でないので、Publicを推奨します。

---

## 7. 自動実行について

以降は **毎朝6時（日本時間）に自動で更新されます。** 何もする必要はありません。

- 時刻を変えたい場合は `.github/workflows/build.yml` の `cron: '0 21 * * *'` を編集します。
  UTC表記なので、日本時間から9を引いた値を書きます（JST 6:00 → UTC 21:00 前日）。
- **cronは指定時刻ぴったりには動きません。** GitHub側の混雑により5〜30分遅れることがあり、
  ごく稀にスキップされます。「6時台に更新されていればOK」くらいに考えてください。
- Publicリポジトリなら Actions の実行時間は無料・無制限です。課金は発生しません。

---

## うまくいかないときは

| 症状 | 原因と対処 |
|---|---|
| Actionsタブにワークフローが出ない | `.github/workflows/build.yml` がアップロードされていない。手順3をやり直す |
| 実行が赤くなる（失敗） | ログを開いてエラー行を確認する。`npm run build` で落ちていればパーサの問題 |
| ページが404 | Settings → Pages の Source が **GitHub Actions** になっているか確認 |
| 作品名や時刻が明らかに変 | 映画.com側の構造が変わった可能性。`build.mjs` の `pushToken()` の正規表現を見直す |
| 一部の館が欠けている | ログの `△ 取得できなかった館` を確認。キャッシュが4回とも直らなかったケース |

---

## 館を追加・削除したくなったら

`theaters.json` をGitHub上で直接編集できます。

1. リポジトリで `theaters.json` をクリック
2. 右上の鉛筆アイコン（Edit this file）
3. 行を足すか消す
4. **Commit changes**

必要なのは映画.comの劇場ページURL（`/theater/13/{エリアID}/{劇場ID}/` の形）だけです。
コードは触りません。翌朝の実行から反映されます。
